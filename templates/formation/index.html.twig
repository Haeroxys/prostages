{% extends 'base.html.twig' %}

{% block title %}Liste des formations{% endblock %}

{% block contenu %}
    <h1>Liste des formations</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NomCourt</th>
                <th>NomLong</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for formation in formations %}
            <tr>
                <td>{{ formation.id }}</td>
                <td>{{ formation.nomCourt }}</td>
                <td>{{ formation.nomLong }}</td>
                <td>
                    <a href="{{ path('formation_show', {'id': formation.id}) }}">Détails</a>
                    <a href="{{ path('formation_edit', {'id': formation.id}) }}">Modifier cette formation</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">aucune formation trouvée</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('formation_new') }}">Créer une nouvelle formation</a>
{% endblock %}
